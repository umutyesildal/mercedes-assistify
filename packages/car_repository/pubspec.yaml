name: car_repository
description: manages car repository
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  uuid: ^3.0.2
  equatable: ^2.0.0
  firebase_client:
    path: ../firebase_client
dev_dependencies:
  flutter_test:
    sdk: flutter
